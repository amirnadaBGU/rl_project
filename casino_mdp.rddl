domain casino_mdp {
    requirements = { reward-stochastic, cpf-stochastic };


    pvariables {
        S : { state-fluent, real, default = 1.0 };
        A : { action-fluent, real, default = 0.0 };
    };

    cpfs {
        S' = Bernoulli(0.5) * A + (1 - A) * S;
    };

    reward = A * 1;
}
